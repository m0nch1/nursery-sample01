extends ./layouts/_home.pug

block vars
  include _config.pug
  - var key = "home"
  - var page = pages[key]

block contents
  .container
    .hero
      .hero__img
      .hero__caption
        .hero__cap--one すくすく
        .hero__cap--two のびのび
        .hero__cap--three もちもち
      a(href="#").hero__scroll Scroll
      .hero__sns
        .hero__icon T
        .hero__icon I
        .hero__icon L
